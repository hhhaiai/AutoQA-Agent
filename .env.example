# AutoQA default env (example)
# Copy to .env and adjust as needed.

# Optional: default environment name. If set, autoqa will load .env.<AUTOQA_ENV> after loading .env.
AUTOQA_ENV=test

# Base URL for the main app (used to resolve Navigate to /path)
AUTOQA_BASE_URL=https://www.saucedemo.com

# Optional: base URL for a separate login domain (used in specs as {{LOGIN_BASE_URL}})
# AUTOQA_LOGIN_BASE_URL=https://login.example.com

# Optional: artifacts & context modes
# AUTOQA_ARTIFACTS=fail
# AUTOQA_TOOL_CONTEXT=screenshot

# Optional: Test credentials (used as {{USERNAME}} / {{PASSWORD}} in specs)
# For SauceDemo example, these are public demo credentials. Do NOT commit real credentials.
AUTOQA_USERNAME=standard_user
AUTOQA_PASSWORD=secret_sauce
